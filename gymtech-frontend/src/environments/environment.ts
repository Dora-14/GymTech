export const environment = {
  production: false,
  apiUrl: 'http://localhost:5000/api',
  // Set to true to bypass login and access dashboards without a running backend.
  // Must be false before connecting to the real backend.
  devBypassAuth: true
};
